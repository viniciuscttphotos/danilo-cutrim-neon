# Project workflow

- Use GitHub CLI (`gh`) for repository creation and GitHub operations whenever applicable.
- Use Vercel CLI (`vercel`, preferably through `npx vercel`) for linking, previewing, and production deployments whenever applicable.
- Never commit local Vercel credentials or the `.vercel/` directory.

